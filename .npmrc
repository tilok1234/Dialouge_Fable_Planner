# pnpm settings (read across pnpm 10 and 11).
# Approve the postinstall scripts of these dependencies:
#   esbuild       — fetches its native binary, needed by vitest
#   unrs-resolver — the TypeScript import resolver used by eslint-plugin-import
onlyBuiltDependencies[]=esbuild
onlyBuiltDependencies[]=unrs-resolver
