# M0 — Product contract (formal) + scaffolding

- **Started:** 2026-08-05
- **Branch:** `m0-scaffolding`
- **Phase 0 sign-off:** recorded in `docs/OPEN_QUESTIONS.md` §7 (all questions
  resolved 2026-08-05).

## Deliverables

- [x] pnpm monorepo skeleton: `pnpm-workspace.yaml`, root `package.json` with
      shared scripts (typecheck, lint, test, validate-samples, build).
- [x] Every package folder under `packages/` with its own `package.json` +
      `tsconfig.json`, wired into the workspace. (`apps/studio/` is scaffolded
      as an empty placeholder in M0; its real UI work is M2.)
- [x] `@df/core`: stable-ID helpers, versioning, **real `contentHash` hashing**
      (resolves Q-F3), provenance helpers. Hash is deterministic over meaningful
      content excluding `version` and timestamps (ARCHITECTURE §7).
- [x] ESLint import-boundary rule enforcing `docs/REPO_LAYOUT.md` §3 (constraint
      #9: core logic never imports a vendor client; `schemas` imports nothing of
      ours). **Verified by planting a `node:fs` import in `@df/core` and
      confirming the rule rejects it.**
- [x] CI config: GitHub Actions, matrix **Windows + Linux** (mitigates R-6),
      running typecheck + lint + test + validate-samples with **no network**.
- [x] Phase-0 docs relocated to `docs/` (resolves Q-D3). Root keeps only
      `README.md`, `LICENSE`, config files.
- [x] README note documenting the `.js` import-specifier convention (Q-E4).

## Acceptance tests (must pass — ACCEPTANCE §M0)

- [x] `[unit]` every package typechecks under strict TS (`pnpm typecheck` clean).
- [x] `[unit]` every package's (possibly empty) test suite passes (`pnpm test`).
- [x] `[contract]` constraint #9 import rule active and passing across the repo.
- [x] `[unit]` `@df/core` hashing is deterministic: same content → same hash,
      and changes when content changes but NOT when only `version`/timestamps do.
- [x] `[schema]` `pnpm run validate-samples` still reports 23/23 (no schema
      drift during scaffolding).
- [x] `[C1-C6]` cross-cutting: typecheck clean, tests green offline, sample
      validates, no new `any` in logic packages, import-boundary lint passes,
      no doc/code contradictions.

**Verification run (2026-08-05):** `pnpm run ci` exits 0. 37 tests pass
(15 schema + 22 core), lint clean, 23/23 samples validate. No network used.

## Human gate

> *You approve the repository layout as built (does the real on-disk tree
> match `REPO_LAYOUT.md`?) and the hashing/versioning scheme.*

- **Status:** [x] signed off by **owner** on **2026-08-05**.
  - Tree matches `docs/REPO_LAYOUT.md`: root → `docs/`, `milestones/`, 8
    `packages/*`, `apps/studio/`, `samples/`, config. Empty packages wired with
    correct, lint-enforced dependency directions.
  - Hashing scheme approved: SHA-256 over canonicalized JSON (sorted keys,
    arrays ordered; excludes `version`/`contentHash`/`createdAt`/`updatedAt`).
    `node:crypto`, no dependency. 22 tests incl. the four metadata-exclusion
    cases.

## Amendment notes

(none yet — record any deviation from the Phase-0 docs here, with reason.)
